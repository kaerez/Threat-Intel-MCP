# Gitleaks configuration for CVE-MCP
title = "CVE-MCP Gitleaks Config"

[extend]
useDefault = true

[allowlist]
description = "Allowlist for false positives"
paths = [
    '''.env.example''',
    '''tests/''',
]

# Allow test credentials
regexes = [
    '''test_password''',
    '''changeme''',
]
