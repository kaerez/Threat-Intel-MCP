name: Build MCPB Bundle

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  mcpb:
    uses: kaerez/mcp-server-template/.github/workflows/mcpb-bundle-python.yml@main
    with:
      server-name: threat-intel-mcp
      src-dirs: src run.py
